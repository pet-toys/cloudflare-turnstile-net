name: Bug report
description: Report incorrect or unexpected behavior in the package.
title: "[Bug]: "
labels: ["bug"]
type: Bug
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to file a bug report. Please fill out the
        sections below so the issue can be reproduced and triaged quickly.
  - type: textarea
    id: description
    attributes:
      label: Description
      description: What happened, and what did you expect to happen instead?
    validations:
      required: true
  - type: textarea
    id: reproduction
    attributes:
      label: Reproduction
      description: >-
        A minimal code sample showing how the verification is configured and
        called — options registration, the attribute or service call, and the
        observed result.
      render: csharp
    validations:
      required: true
  - type: input
    id: package-version
    attributes:
      label: Package version
      description: Which version of PetToys.CloudflareTurnstileNet are you using?
      placeholder: "10.0.1"
    validations:
      required: true
  - type: dropdown
    id: target-framework
    attributes:
      label: Target framework
      description: Which target framework does the affected project use?
      options:
        - net8.0
        - net9.0
        - net10.0
        - Other (describe below)
    validations:
      required: true
  - type: textarea
    id: context
    attributes:
      label: Additional context
      description: Anything else that might help — logs, environment details, screenshots.
    validations:
      required: false
